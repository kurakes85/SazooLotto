package com.sazoo.lotto.ui.theme

import androidx.compose.ui.graphics.Color

// 기본 종이/연필 느낌
val PaperBackground = Color(0xFFF9F5EC)   // 아이보리 종이
val PaperSurface = Color(0xFFFFFFFF)      // 흰 종이
val PencilLine = Color(0xFFB0B0B0)        // 연필선
val PencilDark = Color(0xFF333333)        // 진한 연필색
val PencilLight = Color(0xFF777777)       // 옅은 연필색

// 기본 포인트 (중립)
val SoftGold = Color(0xFFFFE082)

// 오행 포인트 색 (파스텔)
val FireAccent = Color(0xFFFF8A65)   // 화(火)
val WaterAccent = Color(0xFF4FC3F7)  // 수(水)
val WoodAccent = Color(0xFF81C784)   // 목(木)
val MetalAccent = Color(0xFFFFD54F)  // 금(金)
val EarthAccent = Color(0xFFA1887F)  // 토(土)
